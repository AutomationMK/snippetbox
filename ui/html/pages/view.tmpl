{{define "title"}}Snippet #{{.Snippet.ID}}{{end}}

{{define "main"}}
{{with .Snippet}}
<div class="flex flex-col bg-blue-100 border-2 border-blue-900 container mx-auto px-4">
    <div class="flex py-2 px-4 items-center border-b-2 border-blue-900 justify-between">
        <strong>{{.Title}}</strong>
        <span>#{{.ID}}</span>
    </div>
    <pre><code>{{.Content}}</code></pre>
    <div class="flex py-2 px-4 items-center border-t-2 border-blue-900 justify-between">
        <time>Created: {{.Created}}</time>
        <time>Expires: {{.Expires}}</time>
    </div>
</div>
{{end}}
{{end}}
